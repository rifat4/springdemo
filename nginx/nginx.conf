events {}

http {
    upstream app_backend {
        server host.docker.internal:8080 weight=9;
        server host.docker.internal:8081 weight=1;
    }

    server {
        listen 80;
        location / {
            proxy_set_header Host $host;
            proxy_pass http://app_backend;
        }
    }
}

